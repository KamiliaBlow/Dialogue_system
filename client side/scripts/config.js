const AppConfig = {
    API_URL: 'https://DOMENHERE:3000/api',
    ITEMS_PER_PAGE: 10,
    CACHE_HEADERS: {
        'Cache-Control': 'no-cache, no-store, must-revalidate',
        'Pragma': 'no-cache',
        'Expires': '0'
    }
};

export default AppConfig;
